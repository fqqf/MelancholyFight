1. Генерация одного чанка (от 0 до 2000)
 - Сгенерировался чанк 1994. На конце чанка остается платформа, всегда

2. Создание предметов на основе чанка (от 0 до 1994) + Считает пропуски

3. Когда останется N до 1994.
 - Генерируется еще один чанк (от 1994 до 2994). Чанк начинается с 1994, всегда начинается с допустимого GAP [Если chunk_amount!=0, начинать с GAP]

Так же Game хранит предыдущий чанк, если Position > его позиция + размер + N, то удалить весь чанк

4. Zones - список зон, которые могут генерироваться

2. Также до старта генерации, подбрасывается кубик, определюящий уникальные зоны '''

Флаги: generate_unique, generate_gap